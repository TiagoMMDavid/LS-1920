package pt.isel.ls.model.paths;

public class PathTemplate implements Comparable<Path>{

    public PathTemplate(String template) {

    }

    // May be inside Path, please checkj
    @Override
    public int compareTo(Path o) {
        return 0;
    }
}
