package pt.isel.ls.model.commands.common;

/**
 * Enum containing every type of Method. New Methods must be added here.
 */
public enum Method {
    POST,
    GET,
    EXIT,
    OPTION,
    DELETE,
    PUT,
    LISTEN
}
